services:
  tax-id-api:
    build: .
    container_name: tax-id-api
    ports:
      - "8000:8000"
    deploy:
      resources:
        limits:
          memory: 1G # data.csv > 300MB
    restart: always
